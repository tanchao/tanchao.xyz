# Blog Ideas

Internal notes on topics to research and potentially write up as posts. Not published, not rendered on the site — plain backlog for future deep-dives.

| # | Topic | Why interesting | Notes / leads | Status |
|---|-------|------------------|----------------|--------|
| 1 | Streaming architecture in current model serving and agentic systems | Worth digging into how token/event streaming actually works under the hood across model-serving stacks and agent frameworks (vs. just consuming SSE at the API level) | Drafted as `src/content/posts/2026-07-23-how-streaming-works-in-llm-chat-and-agentic-systems.md` — covers TTFT/ITL and disaggregated serving, the new agentic streaming challenges, generalizing partial/streaming beyond JSON, and comparisons to Alexa (ASR/barge-in), Tesla FSD (safety gating), and Netflix (ABR buffering) | drafting |
| 2 | Why some AI demos "resolve" text in parallel instead of typing it token-by-token | Saw a demo where output appeared to render far faster than typical token-by-token — the mechanism is the story. Two reasons text can look faster: it was *generated* faster (throughput) or *rendered* faster (UI trick). The parallel-resolve visual is category one, and specifically the one thing autoregressive models can't do. | **Resolved:** half-remembered "Inflection/Reflection AI" = **Inception Labs** (Mercury dLLM); the same visual is Google's **Gemini Diffusion** (I/O, May 2025). Both are diffusion LLMs generating whole blocks in parallel. Confirmed from vendor pages: Mercury 5–7× throughput, sub-300ms TTFT; Gemini Diffusion 1,479 tok/s. Mechanism taxonomy: (1) diffusion LLMs = true parallel gen; (2) speculative decoding = draft+verify, this absorbs old idea #3; (3) multi-token heads (Medusa/EAGLE, DeepSeek-V3 MTP); (4) lookahead decoding; (5) client-side render tricks. Drafted as `src/content/posts/2026-07-30-why-some-ai-demos-resolve-text-instead-of-typing-it.md`. | drafting |
| 3 | ~~Draft model output tokens as input to a more powerful orchestrating model~~ **(folded into #2)** | This is speculative decoding / model cascades — a draft model proposes tokens the target verifies in one pass. Not a separate discovery. | Merged into #2 as the "speculative decoding" section; the "Modal podcast" attribution can be chased there if a primary source is needed. | dropped |
| 4 | Sandboxing for agentic code execution | Coding agents increasingly need to run untrusted or agent-generated code/commands safely — worth digging into the isolation mechanisms (gVisor, Firecracker microVMs, WASM, seccomp/namespaces) and how agents like Codex and Claude Code scope their exec/shell tools | — | idea |
| 5 | AI-enabled SDLC | The software development lifecycle is being rewritten end-to-end by agents — not just "AI writes code," but how ideation, spec, implement, review, ship, observe, and cleanup change when AI is in the loop. Where does human judgment stay load-bearing? Where does the bottleneck move? | Seed: Substack note contrasting vibe-coding vs real-coding loops (`idea → chat → ai impl+test → try` vs `prototype loop → spec → ai code+param protect+metrics → ORR → param enablement/cleanup`). Also the "self-enforcement loop / quarantine myself to interact more with agent" note. Expand into a full SDLC map (plan → design → code → review → test → ship → operate) with what AI owns vs what humans still gate | idea |
| 6 | Design reviews in the AI era | Design review used to gate human-written designs before expensive implementation. If AI can generate and iterate designs (and code) cheaply, what should a design review actually check — and who is accountable when the design was co-authored with a model? | Seed: Substack note on design-review integrity ("trust me bro" / 100x claims without mechanism). Related: existing design-philosophy post. Angle: what changes when reviewers are reviewing AI-assisted specs, when agents can draft alternatives in minutes, and when the cost of a wrong design is no longer "weeks of eng" but "silent wrongness at scale" | idea |
| 7 | Output format / schema definition in AI agent systems | Agents need structured outputs (tool calls, plans, artifacts) — how do systems actually specify the schema of the object the model must produce? JSON Schema vs grammar-constrained decoding vs tool/function schemas vs Pydantic/Zod-style typed contracts; where validation happens (prompt-only vs decode-time vs post-hoc); and what breaks when nested/optional fields and streaming interact. | Leads: OpenAI Structured Outputs / response_format, Anthropic tool_use schemas, Outlines/Guidance/LMQL constrained decoding, instructor, BAML, TypeChat. Compare prompt-level schema hints vs CFG/grammar masks vs JSON Schema validators. Tie to agent frameworks that treat "typed object out" as the unit of composition. | idea |
| 8 | Declarative language survey — history and open policy languages | Survey how declarative languages evolved (logic programming → config DSLs → policy-as-code) and what the common open policy languages are today — when each fits, and how they differ from imperative code for authorization, compliance, and infrastructure rules. | Leads: lineage through Prolog/Datalog, SQL-as-declarative, Make/Puppet/Terraform; policy languages: OPA/Rego, Cedar (AWS), XACML, Sentinel, Cue, Dhall, JSON Schema as weak declarative. Angle: why agents + policy engines keep colliding (declarative constraints vs generative free-form). | idea |

## Status legend

- `idea` — captured, not yet researched
- `researching` — actively digging in
- `drafting` — turning into a post
- `posted` — shipped, link the post
- `dropped` — decided not to pursue
